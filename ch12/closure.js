// The reason that functions in Egg have access to outside variables is that 
// the local scope of the functin is built from the global scope so that 
// every global variable in existance at the time of building is available 
// to the function.